{
  "name": "remoteg-all-in-one",
  "version": "1.0.0",
  "description": "RemoteG Desktop Host, Web Controller, and Signaling Server",
  "main": "server/index.js",
  "engines": {
    "node": ">=20.12.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.11.0",
    "socket.io": "^4.7.5"
  },
  "scripts": {
    "build": "npm --prefix controller-web install && npm --prefix controller-web run build && npm --prefix server install",
    "start": "node server/index.js"
  }
}
